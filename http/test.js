const successCode = 200 // 成功的code
const tokenExpireCode = 301 // token 过期的code (举例)
// const overtimeCode = 303 // 请求超时了的code,模拟


const currentCode = tokenExpireCode // 当前想到测试的状态
export {
  successCode,
  tokenExpireCode,
  // overtimeCode,
  currentCode
}
